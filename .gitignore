# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/* .links{
    position: fixed;
    width: 100vw;
    top: 0px;
    left: 0px;
    background-color: white; 
    z-index: 1000; 
}
.linkCabeicalho{
 display: flex;
    justify-content: space-between;
    width: 90%;
    margin: 0px auto; 
}
.links img{
    height: 140px;
    height: 80px;
}
.links nav{
    display: flex;
    align-items: center;
}
.links ul{
    display: flex;
    position: fixed;
    top: 22px;
    right: 63px;
}
.links li{
    margin-left: 13px;
    font-size: 17px;
    font-weight: 700;
}
.links a:hover{
    color: rgb(9, 128, 0)
}

.nome{
    margin-left: 13px;
     font-size: 17px;
    font-weight: 600;
    display: flex;
    align-items: center;
}
.nome:hover{
    color: rgb(9, 128, 0)
}

.linkcurso{
    display: flex;
    flex-direction: column;
    width: 112px;
    margin-top: 29px;
    margin-right: 66px;
    padding: 7px 4px;
    background-color: #ededec;
    border: 1px solid #d7d7d3;
    border-radius: 12px
}
.linkcurso li{
    margin-top: 2px ;
}

/* novo 
.linkCabeicalho {
  display: flex;
  justify-content: space-between; 
  align-items: center;
  padding: 10px 20px; /
}

.mainNav ul {
  list-style: none;
  display: flex; 
  gap: 20px; 
}

.mainNav ul li {
  position: relative; 
}

.linkcurso {
  position: absolute;
  top: 100%;
  left: 0;
  background-color: white; /* Cor de fundo do submenu */
  border: 1px solid #ccc;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 5px;
  z-index: 10;
}

.nome {
  cursor: pointer;
  display: flex;
  align-items: center;
}

/* Estilos para o botão do menu hamburger */
.menuHamburger {
  display: none; /* Oculta o botão por padrão em telas maiores */
  background: none;
  border: none;
  cursor: pointer;
  font-size: 24px; /* Tamanho do ícone */
  color: #333; /* Cor do ícone */
}

/* Media Query para telas pequenas */
@media (max-width: 500px) {
  .menuHamburger {
    display: block; /* Mostra o botão hamburger em telas pequenas */
  }

  .mainNav {
    display: none; /* Oculta o menu de navegação padrão */
    flex-direction: column; /* Itens do menu empilhados verticalmente */
    position: absolute;
    top: 70px; /* Ajuste conforme a altura do seu cabeçalho */
    left: 0;
    width: 100%;
    background-color: white; /* Fundo do menu quando aberto */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    z-index: 100;
  }

  .mainNav.menuAberto {
    display: flex; /* Mostra o menu quando a classe menuAberto está ativa */
  }

  .mainNav ul {
    flex-direction: column;
    padding: 10px 0;
  }

  .mainNav ul li {
    padding: 10px 20px;
    border-bottom: 1px solid #eee;
  }

  .mainNav ul li:last-child {
    border-bottom: none;
  }

  .linkcurso {
    position: static; /* Ocupa o espaço normal no fluxo quando o menu principal está aberto */
    box-shadow: none;
    border: none;
    padding-left: 20px; /* Recuo para o submenu de Cursos */
  }
} */
